/* 
 * Uma imagem discreta de largura w e altura h, pode ser representada em um computador
 * através de uma matriz I[i, j], de ordem w x h, que armazena em cada posição um número
 * inteiro entre 0 e 255, o qual especifica uma certa cor em uma paleta de cores. Em pacotes de
 * pintura interativos, é muito comum haver uma operação que efetua o preenchimento de certa
 * área de uma imagem contendo uma cor canterior com uma nova cor cnova. Esta operação pode
 * ser realizada de forma simples através de um método denominado Boundary-fill.
 */
#include <iostream>
// inclusão de alguns metodos do namespace std
	using std::cout;
	using std::cin;
int main(){
 	return 0;
}